/**
 * Describe <%= serviceCls %> here.
 *
 * @param {EventBus} eventBus
 */
export default function <%= serviceCls %>(eventBus) {

}

/**
 * Add names of dependencies here for minification-safety.
 */
<%= serviceCls %>.$inject = [ 'eventBus' ];
